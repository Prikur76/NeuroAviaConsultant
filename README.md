

# ✈️ Neuro Avia Consultant: Нейро-консультант по авиационному страхованию

Интеллектуальный API-сервис на базе **FastAPI** и **GPT-4o-mini**, предназначенный для консультирования по «Правилам страхования ответственности аэропортов и авиационных товаропроизводителей».

## 🌟 Основные возможности

- **RAG-архитектура:** Поиск ответов выполняется строго по предоставленной базе знаний (Google Doc).
- **Экспертный режим:** Нейросеть отвечает как практикующий специалист с 15-летним стажем, не упоминая внешние источники.
- **Динамический мониторинг:** Главная страница с Real-time счетчиком обращений (обновляется без перезагрузки через JS).
- **Lifespan управление:** Автоматическая загрузка и индексация базы знаний при старте сервера.
- **Интерактивная документация:** Полная поддержка Swagger UI и ReDoc.

## 🛠 Технологический стек

- **Backend:** FastAPI (Python 3.10+)
- **LLM:** OpenAI GPT-4o-mini (через ProxyAPI)
- **Векторная база:** FAISS
- **Эмбеддинги:** OpenAI Embeddings
- **Frontend:** HTML5, Tailwind CSS, JavaScript (AJAX)

## 🚀 Быстрый старт

### 1. Подготовка окружения
Создайте файл `.env` или установите переменную окружения:

```bash
export OPENAPI_API_KEY='ваш_ключ_здесь'
export OPENAPI_BASE_URL='ваш_эндпойнт_здесь'

```

Пример в файле [.env.example](.env.example)

### 2. Установка зависимостей

```bash
uv add "fastapi[standart]" uvicorn httpx openai langchain langchain-openai langchain-community faiss-cpu environs

```

### 3. Запуск сервера

```bash
fastapi dev main.py
```

или

```bash
uvicorn main:app --reload
```

После запуска проект будет доступен по адресу: [http://127.0.0.1:8000]()

## 📌 Основные эндпоинты

* `GET /` — Главная страница с красивым интерфейсом и живым счетчиком.

![img](img\main_window.jpg)

* `POST /ask` — Задать вопрос нейро-консультанту.
* *Пример JSON:* `{"question": "Каков размер страховой выплаты?"}`

![img](img\swagger_screenshot.jpg)

* `GET /stats` — Технический эндпоинт со статистикой обращений (JSON).
* `GET /docs` — Интерактивная документация Swagger.

## 🧬 Логика работы (RAG)

Приложение реализует паттерн **Retrieval-Augmented Generation**:

1. **Загрузка:** При старте скачивается документ с правилами.
2. **Chunking:** Текст разбивается на части по 1000 символов.
3. **Indexing:** Создаются векторы фрагментов и сохраняются в FAISS.
4. **Query:** При вопросе система находит 4 самых релевантных куска текста и передает их в LLM вместе с экспертным промптом.

---

### Лицензия: [MIT License](LICENSE)

---

*Проект разработан в рамках учебного задания по оптимизации промптов и созданию API [Университета искусственного интеллекта](https://neural-university.ru/)*

